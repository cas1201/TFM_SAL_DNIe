<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ColeHop.Views.PendingApprovalsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodel="clr-namespace:ColeHop.ViewModels"
    xmlns:model="clr-namespace:ColeHop.Models"
    xmlns:utils="clr-namespace:ColeHop.Helpers"
    xmlns:res="clr-namespace:ColeHop.Resources.Strings"
    x:DataType="viewmodel:PendingApprovalsViewModel">

    <RefreshView
        IsRefreshing="{Binding IsRefreshing}"
        Command="{Binding RefreshCommand}">

        <Grid RowDefinitions="Auto,*">
            <Label Grid.Row="0" Margin="16,20,16,0"
                   Style="{StaticResource SectionTitle}"
                   Text="{x:Static res:AppResources.PendingApprovals}" />
            <CollectionView Grid.Row="1" ItemsSource="{Binding PendingChildren}" Margin="16">
                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="8" Padding="0,40">
                        <Label
                            Style="{StaticResource EmptyStateTitle}"
                            Text="{x:Static res:AppResources.NoPendingApprovals}" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Child">
                        <Border
                            Style="{StaticResource Card}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource TertiaryTintLight}, Dark={StaticResource TertiaryTintDark}}"
                            Margin="0,6">
                            <Grid ColumnDefinitions="*,Auto,Auto" RowDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="12">
                                <Label
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Text="{Binding FullName}"
                                    FontSize="16"
                                    FontAttributes="Bold"
                                    TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource Gray200}}" />
                                <Label
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Text="{Binding Grade}"
                                    FontSize="13"
                                    TextColor="{DynamicResource Gray600}" />
                                <Label
                                    Grid.Row="2"
                                    Grid.Column="0"
                                    Text="{Binding TutorFullName, StringFormat='Tutor: {0}'}"
                                    FontSize="13"
                                    TextColor="{DynamicResource Gray600}" />
                                <Label
                                    Grid.Row="3"
                                    Grid.Column="0"
                                    Text="{Binding TutorDni, StringFormat='DNI: {0}'}"
                                    FontSize="13"
                                    TextColor="{DynamicResource Gray600}" />

                                <ImageButton
                                    Grid.Row="0"
                                    Grid.RowSpan="4"
                                    Grid.Column="1"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    CornerRadius="20"
                                    BackgroundColor="{DynamicResource Success}"
                                    Padding="8"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:PendingApprovalsViewModel}}, Path=ApproveCommand}"
                                    CommandParameter="{Binding .}">
                                    <ImageButton.Source>
                                        <FontImageSource
                                            FontFamily="MaterialSymbolsRounded"
                                            Glyph="{x:Static utils:MaterialIcon.Check}"
                                            Size="20"
                                            Color="White" />
                                    </ImageButton.Source>
                                </ImageButton>

                                <ImageButton
                                    Grid.Row="0"
                                    Grid.RowSpan="4"
                                    Grid.Column="2"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    CornerRadius="20"
                                    BackgroundColor="{DynamicResource Danger}"
                                    Padding="8"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:PendingApprovalsViewModel}}, Path=RejectCommand}"
                                    CommandParameter="{Binding .}">
                                    <ImageButton.Source>
                                        <FontImageSource
                                            FontFamily="MaterialSymbolsRounded"
                                            Glyph="{x:Static utils:MaterialIcon.Close}"
                                            Size="20"
                                            Color="White" />
                                    </ImageButton.Source>
                                </ImageButton>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                </CollectionView>

            <ActivityIndicator
                IsRunning="{Binding IsBusy}"
                IsVisible="{Binding IsBusy}"
                VerticalOptions="Center"
                HorizontalOptions="Center" />
        </Grid>
    </RefreshView>
</ContentPage>
